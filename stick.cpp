#include <iostream>
#include <iomanip>
#include <cassert>
#include <cmath>
#include <cstdio>
#include <cstring>
#include <cstdlib>
#include <map>
#include <set>
#include <queue>
#include <stack>
#include <vector>
#include <chrono>
#include <algorithm>

using namespace std;

int main()
{
    vector<int>v = {5,4,4,2,2,8};

    // int n = v.size();
    // sort(v.begin(),v.end());
    // for(int i = 0; i < n; i++)
    // {
    //    cout << v.size();
    //     v[i] = v[i] - v[0];
    //     if(v[i] != 0)
    //     v.push_back(v[i]);

    //     for(auto j =v.begin() ; j < v.end(); j++)
    //     {
    //         v[*j] = v[*j] = v[0];
    //         if(v[*j] == 0)
    //         v.erase(j);
    //     }

    // string s = "ankush";
    // string s1;
    // s1 = s;
    // cout << s;
    // for(int i = 0; i < s1.size(); i++)
    // {
    //     char c = s1[i];
    //     s.push_back(s[i]);
    // }
    
    return 0; 
}